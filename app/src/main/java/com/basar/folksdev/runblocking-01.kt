package com.basar.folksdev

import kotlinx.coroutines.*

/*
fun main() {
    println("Before runBlocking builder, Thread :" + Thread.currentThread().name)
    runBlocking {
        println("Start of runBlocking builder, Thread :" + Thread.currentThread().name)
        delay(5000L)
        println("End of runBlocking builder, Thread :" + Thread.currentThread().name)
    }
    println("After runBlocking builder, Thread :" + Thread.currentThread().name)
}
*/
